n1 = float(input('Digita a primeira nota: '))
n2 = float(input('Digite a segunda nota: '))
media = (n1 + n2) / 2
print('A média entre {} e {} é igual a {}'.format(n1, n2, media))

# float porque a nota geralmente não é um número inteiro, pode ser 7.5 8.8 9.1, e não apenas 7, 8 ou 9.

